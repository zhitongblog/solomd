# 欢迎使用 SoloMD ✏️

谢谢你试用 SoloMD —— 一个用 Tauri 2 写的、原生快、专注 Markdown 的编辑器。

这份导览有 **4 个简短的标签页**。从任意一个开始读都行。

## 现在打开的几个文档

| 标签 | 内容 |
| --- | --- |
| **欢迎** | 当前页 |
| **Markdown 语法** | 标题、列表、表格、代码、数学、Mermaid 速查 |
| **演讲模式** | 怎样把文档变成全屏 PPT |
| **快捷键** | 真正用得上的那些 |

## 60 秒上手

1. 直接开始打字，内容暂存在内存里。`Ctrl+S`（Mac 上 `Cmd+S`）保存到磁盘。
2. `Ctrl+Shift+P` 在 **编辑 → 分栏 → 预览** 之间循环切换。
3. 直接把图片拖进编辑器：会插入相对路径（可用 front-matter 的 `imageRoot` 指定保存位置）。
4. 通过 `文件 → 打开文件夹…` 让侧边树管理你的笔记。

## SoloMD 的特别之处

- **原生打印转 PDF**：`Ctrl+P` 走系统打印对话框，不走 headless Chrome。
- **演讲模式**：`Ctrl+Alt+P` 全屏播放当前文档，`---` 行分页。
- **Front matter 的 `imageRoot`**：每篇文章的粘贴/拖拽图片可写到指定目录。
- **App Store 版默认无遥测**，其他渠道默认开但可关 —— 你说了算。

> 提示：按 `Ctrl+K`（Mac 上 `Cmd+K`）打开 **命令面板** —— SoloMD 能干什么，这里都有。

完事。这些教程标签只在内存里，不会写到磁盘，看完直接关掉就行。

— *SoloMD 团队*
