---
import Layout from '../../../layouts/Layout.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import Giscus from '../../../components/Giscus.astro';

const lang = 'zh' as const;
---

<Layout
  title="SoloMD 快速上手 — 安装、打开、书写、保存"
  description="在 macOS、Windows、Linux、iOS 或 Android 上安装 SoloMD,打开你的第一个文件夹,理解标签页与视图模式,并且永远不丢草稿。"
  lang={lang}
>
  <Header lang={lang} />
  <main id="main">
    <article class="container docs-article">
      <nav class="breadcrumbs">
        <a href="/zh/">首页</a> / <a href="/zh/docs/">文档</a> / <span>快速上手</span>
      </nav>

      <h1>快速上手</h1>
      <p class="lead">从下载到保存第一篇笔记，大约只需三分钟。</p>

      <h2>1. 安装</h2>
      <table class="kb-table">
        <thead><tr><th>平台</th><th>方式</th></tr></thead>
        <tbody>
          <tr><th>macOS</th><td><code>brew install --cask zhitongblog/solomd/solomd</code>，或从 <a href="/zh/">solomd.app</a> 下载已签名的通用 <code>.dmg</code>，拖进「应用程序」即可。需要 macOS 10.15+。</td></tr>
          <tr><th>Windows</th><td>下载 <code>.msi</code>（x64 或 arm64）——安装到 <code>C:\Program Files\SoloMD</code>。不想安装的话也有便携版 ZIP。需要 Windows 10+（<a href="/zh/#faq">为什么不支持 Windows 7</a>）。</td></tr>
          <tr><th>Linux</th><td><code>.AppImage</code>、<code>.deb</code> 或 <code>.rpm</code>，均提供 x64 与 arm64。</td></tr>
          <tr><th>iOS / Android</th><td>iOS 15+ 上架 App Store；Android 7+ 可侧载 APK，或加入 Play 商店测试版。</td></tr>
        </tbody>
      </table>

      <h3>Windows:任务栏图标显示空白?</h3>
      <p>
        如果升级后任务栏按钮或固定项的 SoloMD 图标是空白的,那是 Windows
        图标缓存里残留了坏条目——卸载重装也不会清除它。在命令提示符里执行以下四行,然后重新固定即可:
      </p>
      <pre><code>taskkill /f /im explorer.exe
del /f /q %LocalAppData%\IconCache.db
del /f /q %LocalAppData%\Microsoft\Windows\Explorer\iconcache_*.db
start explorer.exe</code></pre>

      <h2>2. 直接开始打字</h2>
      <p>
        首次启动会打开几篇简短的欢迎文档 —— 看一眼或直接关掉都行。
        你可以立刻开始输入：内容先保存在内存中，直到你按下
        <kbd>Ctrl/⌘ S</kbd> 保存；未保存的草稿在应用重启后也会通过会话恢复保留下来。
        未命名的标签页会在第一次保存时询问文件名。
      </p>

      <h2>3. 打开文件或文件夹</h2>
      <ul>
        <li><strong>单个文件：</strong>文件 → 打开（<kbd>Ctrl/⌘ O</kbd>）、把文件拖进窗口，或直接双击 <code>.md</code> 文件 —— SoloMD 会注册为 Markdown 编辑器。</li>
        <li><strong>文件夹（推荐）：</strong>文件 → 打开文件夹。这个文件夹就成了你的<em>工作区</em>：左侧出现文件树，搜索 / 标签 / 反向链接 / 关系图会索引其中的每一篇笔记，并且每个文件夹会各自记住打开过的标签页。</li>
      </ul>

      <h2>4. 标签页与分栏</h2>
      <ul>
        <li>每篇文档就是一个标签页。拖拽可以调整顺序，<kbd>Ctrl/⌘ W</kbd> 关闭。</li>
        <li>在命令面板（<kbd>Ctrl/⌘ Shift K</kbd> → “向右拆分编辑器”）里可以把编辑器向右或向下拆分，并排查看两篇笔记。</li>
        <li>命令面板列出了应用里的每一个操作及其快捷键 —— 拿不准的时候，打开它输入你想做的事就行。它会跟随你的界面语言。</li>
      </ul>

      <h2>5. 选一个视图模式</h2>
      <table class="kb-table">
        <thead><tr><th>模式</th><th>你看到的</th></tr></thead>
        <tbody>
          <tr><th>纯编辑</th><td>带语法高亮的 Markdown 源码。</td></tr>
          <tr><th>实时编辑</th><td>所见即所得：格式就地渲染，光标所在行显示原始源码。表格、公式、Mermaid、图片和内嵌 HTML 都会以块的形式渲染。</td></tr>
          <tr><th>分栏</th><td>左边源码、右边渲染预览，滚动像素级同步。</td></tr>
          <tr><th>预览 / 阅读模式</th><td>只显示渲染结果；阅读模式是为长文准备的全宽衬线排版。</td></tr>
        </tbody>
      </table>
      <p>用 <kbd>Ctrl/⌘ Shift P</kbd> 循环切换模式，或点击工具栏上的模式按钮。</p>

      <h2>6. 保存与安全网</h2>
      <ul>
        <li><kbd>Ctrl/⌘ S</kbd> 写入磁盘。当标签页没有未保存的修改时，被其他程序改动的文件会自动重新加载 —— 有未保存修改的标签页则一定会先询问你。</li>
        <li>可选的<em>失焦自动保存</em>和 <em>Git 历史</em>（自动快照 + 一键回滚）都在设置里 —— 见<a href="/zh/docs/sync/">同步与历史</a>。</li>
        <li>笔记就是文件夹里的普通 <code>.md</code> 文件。没有数据库，没有锁定 —— 任何其他工具都可以直接指向同一个目录。</li>
      </ul>

      <h2>下一步</h2>
      <ul>
        <li><a href="/zh/docs/editor/">编辑器详解</a> —— 实时渲染、图片、表格、Vim 模式、打字机模式。</li>
        <li><a href="/zh/docs/workspace/">工作区与组织</a> —— 大纲、搜索、标签、反向链接、保存的视图。</li>
        <li><a href="/zh/docs/sync/">同步、历史与备份</a> —— GitHub 同步，可选端到端加密。</li>
        <li><a href="/zh/docs/ai/">AI 功能</a> —— 用自己的 API Key，或用 Ollama 完全本地运行。</li>
      </ul>

      <Giscus />
    </article>
  </main>
  <Footer lang={lang} />
</Layout>
