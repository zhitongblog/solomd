---
import Layout from '../../layouts/Layout.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import Giscus from '../../components/Giscus.astro';

const lang = 'zh' as const;
const VERSION = '3.5.5';
const baseUrl = `https://github.com/zhitongblog/solomd/releases/download/v${VERSION}`;
---

<Layout
  title="SoloMD v3.5.5 新功能"
  description="潜伏 6 周的 Win UI 中文字体回退终于被定位并修了。匿名遥测整体从 Aptabase 迁到自建 Cloudflare D1 端点。还有 @beihai23 的 PR #48：PDF 导出超时、outline 按钮换位、Mermaid 浮窗对比度。"
  lang={lang}
>
  <Header lang={lang} />
  <main>
    <article class="container whats-new">
      <nav class="breadcrumbs">
        <a href="/zh/">首页</a> / <span>新功能</span>
      </nav>

      <p class="version-row">
        <span class="version-tag">v{VERSION}</span>
        <time>2026 年 4 月</time>
      </p>
      <h1>Win 字体修复、自建遥测、polish。</h1>
      <p class="lead">
        三件事打包发了一个 patch：潜伏 <strong>6 周的 Win UI 中文字体
        回退</strong>终于被定位并修了；匿名遥测整体<strong>从 Aptabase
        迁到自建 Cloudflare D1 端点</strong>（数据通路里没有任何
        第三方 SaaS）；还有 <strong>@beihai23 的一个 polish PR</strong>
        覆盖 PDF 导出超时、outline 按钮位置、Mermaid 浮窗对比度。
      </p>

      <div class="cta-row">
        <a class="btn btn--primary btn--lg" href={`${baseUrl}/SoloMD_${VERSION}_universal.dmg`}>⬇ 下载 v{VERSION} (Mac)</a>
        <a class="btn btn--secondary btn--lg" href={`${baseUrl}/SoloMD_${VERSION}_x64_en-US.msi`}>下载 (Windows)</a>
        <a class="btn btn--secondary btn--lg" href={`https://github.com/zhitongblog/solomd/releases/tag/v${VERSION}`}>所有平台 →</a>
      </div>

      <section>
        <h2>🇨🇳 Win UI 中文字体修复（issue #43）</h2>
        <p>
          从 v0.1.12 起，Win11 用户看到 UI 中文渲染变细变虚，跟原生
          ClearType 不一样。根因：自带的 <code>"SoloMD CJK"</code>
          字体（一个 43&nbsp;KB 的 LXGW WenKai 子集，本来是给 iOS
          模拟器兜底用的）被放到了 font-family chain 的<em>第一位</em>。
          加上它的 <code>unicode-range: U+3400-9FFF</code>，浏览器对
          所有中文字符都优先用它，<em>"YaHei、PingFang、Noto Sans CJK
          全跳过"</em>。
        </p>
        <p>
          修复就一行：把它挪到 chain 末尾。unicode-range 控的是
          下载触发，chain 顺序控的是优先级。每个真实平台都会先找到
          系统 CJK 字体；只有 iOS 模拟器（它本来的目的）才回落到
          这个兜底字体。
        </p>
        <p class="thread-note">
          完整诊断 →
          <a href="/zh/blog/v3-5-5-font-fix-self-hosted-analytics/">v3.5.5 博客</a>。
          Issue 讨论 →
          <a href="https://github.com/zhitongblog/solomd/issues/43">#43</a>。
          报告人 <a href="https://github.com/sparkdonn-netizen">@sparkdonn-netizen</a>。
        </p>
      </section>

      <section>
        <h2>📊 自建匿名遥测 —— Aptabase → Cloudflare D1</h2>
        <p>
          之前用 Aptabase 收集启动数 + 功能使用频率，但 dashboard
          显示只有 ~2 个活跃用户，跟 3,400+ 下载量对不上，没法用。
          没去查这家 SaaS 哪里出问题，直接砍掉，换成 Cloudflare
          Pages Function + D1 自建。
        </p>
        <ul>
          <li>桌面端 POST 到 <code>solomd.app/api/track</code>。匿名设备 UUID 在 localStorage。<strong>没有 IP，没有 User-Agent，没有指纹。</strong></li>
          <li>服务端 validator 丢弃疑似 PII 数据；一律返 204。</li>
          <li>D1 免费额度按当前体量 3 倍冗余（5M reads/day、100k writes/day）。</li>
          <li>管理面板 <code>solomd.app/admin/</code> 用 Bearer token，看每日活跃 / 版本分布 / OS / locale。</li>
        </ul>
        <p>
          退订开关在 Settings → 发送匿名使用数据（默认开）。
          架构说明在仓库的 <code>web/ANALYTICS.md</code>。
        </p>
      </section>

      <section>
        <h2>🐛 PR #48 —— @beihai23 的 4 处 polish</h2>
        <ol>
          <li><strong>PDF 导出 30 秒超时</strong> —— 用 <code>Promise.race</code> 套住整个流程；html2pdf.js / Mermaid 卡死时不再无限冻 UI。DOM 清理逻辑改成幂等。</li>
          <li><strong>Outline 按钮搬到 Tab 上</strong> —— 编辑区左上角浮动 ≌ 按钮去掉了，每个 Markdown tab 自带一个 ≌ 切大纲。</li>
          <li><strong>Mermaid 浮窗对比度</strong> —— lightbox 里的 SVG 自动套主题适配背景卡片（浅色白底，深色深石板）。</li>
          <li><strong>HTML 规范</strong> —— <code>&lt;table&gt;</code> 加上 <code>&lt;tbody&gt;</code>，消 Vite warning。</li>
        </ol>
      </section>

      <section>
        <h2>🔮 下一步 —— v3.6</h2>
        <p>
          Issue <a href="https://github.com/zhitongblog/solomd/issues/44">#44</a>
          —— 实时编辑模式下图片和表格的内联渲染 —— 是下一个要做的。
          这是一个 CodeMirror block-decoration 扩展：光标不在源码行
          时显示渲染后的图片/表格，光标进入则展开成源码。不是改 CSS
          能解决的，是真正的编辑器扩展。再之后：iPad / Mac App
          Store 重新提交还排着队。
        </p>
      </section>

      <p class="thanks-row">
        感谢 <strong>@beihai23（王志刚）</strong>贡献 PR #48 ——
        Ta 的第三个 PR，每个都是高质量修复 ——
        以及 <strong>@sparkdonn-netizen</strong> 提供的截图
        让 #43 真正能定位。早期版本 →
        <a href="/zh/blog/">浏览博客 →</a>
      </p>
    </article>
    <Giscus lang={lang} />
  </main>
  <Footer lang={lang} />
</Layout>

<style>
  .whats-new { max-width: 820px; padding: 40px 20px 80px; }
  .breadcrumbs { font-size: 13px; color: var(--text-muted); margin-bottom: 16px; }
  .breadcrumbs a { color: inherit; }
  .version-row { display: flex; align-items: center; gap: 12px; margin: 0 0 14px; }
  .version-tag {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 11px;
    font-weight: 700;
    padding: 4px 10px;
    border-radius: 999px;
    background: var(--accent);
    color: #000;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }
  .whats-new time { font-size: 13px; color: var(--text-muted); }
  .whats-new h1 { font-size: clamp(2.2rem, 5vw, 3.4rem); line-height: 1.1; margin: 0 0 22px; }
  .whats-new h2 { font-size: clamp(1.5rem, 3vw, 1.9rem); margin: 56px 0 16px; }
  .whats-new .lead { font-size: 1.15rem; color: var(--text-muted); line-height: 1.65; margin-bottom: 28px; }
  .whats-new p { line-height: 1.75; margin: 12px 0; }
  .whats-new ul, .whats-new ol { padding-left: 22px; }
  .whats-new li { margin: 6px 0; line-height: 1.7; }
  .whats-new code {
    background: var(--bg-soft);
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 0.92em;
    font-family: var(--font-mono);
    color: var(--accent-deep);
  }
  /* Inline links accent-colored; excludes CTA buttons so global
     .btn--primary / --secondary color rules win. See en page for why. */
  .whats-new a:not(.btn) { color: var(--accent); }
  .cta-row {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin: 28px 0 8px;
  }
  .thread-note {
    font-size: 13px;
    color: var(--text-muted);
    border-left: 2px solid var(--border);
    padding-left: 14px;
    margin-top: 14px;
  }
  .thanks-row {
    margin-top: 64px;
    padding-top: 22px;
    border-top: 1px solid var(--border);
    font-size: 14px;
    color: var(--text-muted);
  }
</style>
